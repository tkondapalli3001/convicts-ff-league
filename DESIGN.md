# DESIGN.md — Convicts FF League

The design system and architecture reference for the site. CLAUDE.md covers
working rules for AI-assisted development; this file covers *what the system
is* — visual language, component inventory, and how data moves.

---

## Identity

**Statmuse × Sleeper.** A professional sports-analytics archive for a private
league: dark mode only, high-density tables, hero typography for big numbers,
borderless rounded cards. It should feel like a real analytics product that
happens to be about 12 friends, not a hobby page.

Never change the visual theme or remove features without being asked.

## Color palette

Custom Tailwind tokens (see `tailwind.config.ts`), all prefixed `s-`:

| Token | Role |
|---|---|
| `s-bg` (#080c14) | Page background |
| `s-bg2`, `s-bg3` | Card and inner-panel backgrounds |
| `s-border`, `s-border2` | Hairline borders (often `white/[0.05-0.07]` instead) |
| `s-text`, `s-text2`, `s-text3` | Text hierarchy: bright → muted → faint |
| `s-gold` | Accent: records, champions, playoff notes |
| `s-green` / `s-red` | Win / loss semantics |
| `s-teal` | Search & interactive accents |

Brand gradient: violet `#8B5CF6` → blue `#3B82F6` (navbar wordmark, app icons,
OG card). Owner identity colors live in `OWNER_COLORS`; NFL position colors in
`POS_COLORS` / `POS_TEXT_CLASSES` / `POS_BADGE_CLASSES` — all in
`lib/owner-map.ts`, exported via `@/lib/constants`. Never redefine locally.

## Shape & typography tokens

- Cards: `.bento-card` / `.gl` (16px radius, defined in `globals.css`)
- Inner panels: `rounded-[10px]` · Pills: `rounded-full` · Tab buttons: `rounded-[8px]`
- Font: Inter (400–900). Hero numbers are heavy (`font-black`/`font-extrabold`)
  and large; labels are tiny uppercase with wide tracking
  (`text-[10px] font-bold tracking-[2px+] uppercase text-s-text3`)
- Keyboard focus: global `:focus-visible` ring in `globals.css`
- Micro-interactions: `active:scale-[0.98]` on pills/buttons, `animate-fade-in`
  on page/overlay mounts, champion-name `champ-glow` breathing effect

## Shared primitives (`components/shared/`)

| Component | Use |
|---|---|
| `PageHeader` | h1 + subtitle at the top of every page |
| `PillTabs` | Gold-active tab row (6 pages) |
| `StatChip` | Hero number card (home quick stats) |
| `StatBox` | Smaller stat tile |
| `OwnerAvatar` | Owner photo/initials with identity-color glow |
| `PlayerHeadshot` | NFL player photo, position-badge fallback, team logo for DEF |
| `FinishBadge`, `WinPctBadge` | Season finish / win% pills |
| `LoadingSpinner`, `ErrorState` | Standard page loading & error states |

Use these instead of re-rolling markup.

---

## Architecture

### Data flow

```
public/data/season-<year>.json     ← immutable, generated by npm run snapshot
        +
Sleeper API (current season only)
        ↓
context/LeagueContext.tsx          one load on mount → LeagueState
        ↓
lib/data-processing/               raw shapes → Matchup[], OwnerSeason[], …
        ↓
lib/stats/ · lib/preview/ · lib/search/   pure computation engines
        ↓
hooks/ (useMemo wrappers)          → pages render
```

- **Snapshot layer:** completed seasons ship as static JSON. The site makes
  zero Sleeper calls for history; only a non-snapshotted (live) season is
  fetched. If the snapshot files are missing, `LeagueContext` falls back to
  full live fetching — the snapshot is an accelerator, never a dependency.
- **Everything is client-side.** Static export (`output: 'export'`), no SSR,
  no API routes, deployed to GitHub Pages under `basePath /convicts-ff-league`.

### Stat engine (`lib/stats/`)

Single source of truth for career records, championship counts (0.5 shared
titles, `winner.includes(name)` matching), H2H records (ties count for the
perspective owner), and record-book extremes. Locked by vitest tests.

### Search (`lib/search/` + `components/search/`)

Local natural-language query engine — tokenizer → entity matching (owners
with typo tolerance, NFL players) → regex intent table (13 intents) →
resolvers over the stat engine. No LLM, no API keys, by explicit decision.
UI is a ⌘K overlay portaled to `document.body` (the navbar's backdrop-filter
would trap a fixed overlay — don't move it back inside).

### Previews (`lib/preview/` + `app/this-week/`)

Weekly matchup cards: season records/streaks/seeds, career H2H, playoff
implications (win/loss seed movement), deterministic template smack talk
(seeded by `year|week|matchup` — same lines all week), and projected totals.
**Projections use an undocumented Sleeper endpoint** — every failure path
returns null and the card renders without the row. Keep it that way.

---

## Yearly ritual

1. **After the draft (new season created in Sleeper):** update `LEAGUE_ID`
   in `lib/config.ts` to the new season's league id. The This Week tab and
   all live data follow automatically.
2. **After the season ends** (status flips to `complete`): run
   `npm run snapshot`, commit the new `public/data/` files. History is now
   frozen and free.

Deploy is automatic: push to `main` → GitHub Actions → GitHub Pages.
